import SwiftUI

enum AppTheme {
    static let accent = Color.orange
    static let backgroundTop = Color.orange.opacity(0.12)
    static let backgroundBottom = Color.yellow.opacity(0.08)
    static let card = Color.orange.opacity(0.08)
}
